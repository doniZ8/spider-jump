.coin-display {
  display: inline-flex;
  align-items: center;
  gap: 7px;
}

.coin-icon {
  position: relative;
  display: inline-block;
  width: 21px;
  height: 21px;
  border-radius: 50%;
  background: radial-gradient(
    circle at 35% 30%,
    #fff2a8 0%,
    #ffd84d 25%,
    #e9a51f 62%,
    #a96708 100%
  );
  border: 2px solid #ffe27a;
  box-shadow:
    inset 0 0 0 2px rgba(255,255,255,.18),
    0 2px 7px rgba(0,0,0,.35);
  flex: 0 0 auto;
}

.coin-icon::after {
  content: "";
  position: absolute;
  left: 50%;
  top: 50%;
  width: 7px;
  height: 7px;
  transform: translate(-50%, -50%);
  border: 2px solid rgba(134,84,8,.75);
  border-radius: 50%;
}

.coin-icon::before {
  content: "";
  position: absolute;
  left: 4px;
  top: 3px;
  width: 6px;
  height: 3px;
  border-radius: 50%;
  background: rgba(255,255,255,.7);
  transform: rotate(-25deg);
}
